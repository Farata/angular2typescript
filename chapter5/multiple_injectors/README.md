To launch the app install `npm` and `jspm` dependencies:

```sh
npm install
jspm install
```

And start a Web server of your choice in the root directory, in this book we use `http-server`:

```sh
http-server
```
